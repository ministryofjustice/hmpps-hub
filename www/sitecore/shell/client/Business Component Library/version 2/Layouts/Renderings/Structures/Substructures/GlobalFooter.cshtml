@using Sitecore.Mvc
@{
  var footerInfo = Html.Sitecore().Placeholder("GlobalFooter.FooterInfo");
  var isFooterEmpty = string.IsNullOrEmpty(footerInfo.ToString());
}

@if (!isFooterEmpty)
{
  <footer class="sc-globalFooter">
    @Html.Sitecore().Placeholder("GlobalFooter.FooterInfo")
  </footer>
}